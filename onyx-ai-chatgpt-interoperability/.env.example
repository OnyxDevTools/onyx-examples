# Onyx API key (exposed to the browser for this demo)
VITE_OPENAI_API_KEY=replace_me
# Use a relative base to go through the Vite dev proxy (avoids CORS)
VITE_OPENAI_BASE_URL=/api
# Dev proxy target (change to https://api.openai.com/v1 if using OpenAI)
VITE_PROXY_TARGET=https://ai.onyx.dev/v1
